# Changelog

## 2026-09-07

### Added

- 顶栏新增单图标明暗切换按钮，设置同时提供跟随系统、浅色和深色模式；手动切换用短暂柔和揭示降低整屏闪动，并尊重系统的“减少动态效果”设置。主题即时保存到本机偏好，启动阶段提前应用并协调原生窗口底色，表单、筛选、卡片、弹窗、状态色、滚动条、拖拽与更新界面均完成深色适配，打印媒体保持白底黑字。

### Changed

- 筛选条件改为直接在抬头、报账人、标签、高级及对应高级筛选框上显示浅蓝激活态，移除重复的条件标签和高级筛选蓝点；已生效的高级下拉框不再残留额外焦点光晕，同时校正筛选文字对齐，并收敛批次标签的阴影与渐变动效。「清空全部筛选」移到筛选项旁，有条件时明确高亮、无条件时自动禁用。

## 2026-09-06

### Changed

- 条目有多张付款截图时，卡片付款按钮直接显示张数；高级筛选新增「多张（2 张及以上）」条件，便于集中核对分笔付款材料。
- 所有列表视图的多选操作均可「重新识别」，并可分别选择发票或付款截图；软件记录独立于软件版本的本地识别规则版本与文件摘要，普通软件升级不会让已处理材料重新进入待识别。付款截图未识别到金额或识别合计与发票总额不一致时进入「识别提醒」，重新识别不会覆盖已确认的实付金额。
- 修复 Windows 11 下工具栏下拉选项首字左缘被裁切、关闭与清除按钮的悬停底色偏离图标等对齐问题。
- 修复部分数电发票把明细表头与首条商品文字叠在同一文本层时无法识别物资名称的问题，兼容无单位数量的服务/加工明细，以及数量与单价粘连的商品明细。
- 校正北京理工大学购买方税号为 `12100000400009127B`，避免正确发票被误报为「识别提醒」。
- 发票归属校验新增购买方税号核对：北京理工大学应为 `12100000400009127B`，北京理工大学教育基金会应为 `53100000500021676K`；税号缺失、错误，或已知税号与抬头矛盾时均进入「识别提醒」。
- 拖拽或粘贴单个 `.tidoc` 绑定包会直接进入完整性检查和导入预览；从列表、条目卡片或详情区域放入都不会再被当作普通附件。混入其他文件或一次放入多个绑定包时会明确提示分开导入。
- 批次相关界面与文档统一使用「归档 / 已归档」。

## 2026-09-02

### Added

- 可选「OCR 识别组件」：调用阿里云增值税发票识别，补齐明细的数量、规格等字段。与打印导出组件并列，独立版本、独立更新，SDK 不进核心包；识别原文与解析结果永久保存在条目里，查看和比对不重复计费。
- 选中工具条新增「云识别」按钮：识别前预检本次调用张数与计费提示；已有 XML 权威数据的条目默认跳过，无发票 PDF 的条目自动跳过，已有结果的条目提示重复计费。完成后逐条汇报已补齐、待确认与失败明细。
- 识别提醒视图新增建议条：显示当前视图可用云识别补齐的条目数，一键全部识别（自动建议、手动触发，不做后台识别）。
- 条目详情新增「阿里云识别」折叠区：当前值与阿里云值逐字段对照、明细左右比对，可逐项或整套采用；存在待确认差异时卡片显示 OCR 徽标并默认展开，处理完自动消失。
- 设置新增「阿里云 OCR」分区：软件内填写 / 更换 / 清除 AccessKey（仅存本机、界面显示掩码、建议使用只授权文字识别的 RAM 账号 Key），显示组件安装状态与本机累计调用次数。
- 结果应用策略：本地为空的字段自动补齐；PDF 文本来源且金额闭合校验通过时自动修复日期、销售方与整套明细；发票号、总额、抬头相关字段与 XML 来源条目只进待确认，人工改过的值绝不静默覆盖，全部改动留痕「[阿里云OCR]」。

## 2026-08-27

### Changed

- 在当前批次内确认归档后，视图随批次转入「已归档」，不再跳回全部在办。
- 全选按钮与「×」分工明确：「×」只在部分选中时出现用于清空；全部选中时仅保留「取消全选」，不再出现两个同效按钮。
- 界面字体栈调整：Windows 上西文与数字优先使用 Segoe UI、中文回退微软雅黑（UI 字体优先），macOS 不变；移除设置分区标题的单独字体硬编码。
- macOS 核心安装包体积约缩小六成。

### Fixed

- 打印导出组件安装或更新成功后，自动清理同平台旧版本目录，不再随版本升级累积占用磁盘。

## 2026-08-14

### Added

- 批次栏默认「在办」：只属于已归档批次的条目会离开主列表；「在办 / 已归档」在同一位置切换。全库仍拦截重复发票。
- 设置底部增加「说明文档」链接，打开官网使用说明。
- 批次支持「批次备注」：新建 / 编辑时可填写，批次上可通过悬浮图标查看，归档确认和批次栏下方展示。

### Changed

- 搜索框回到工具条第一行，宽度随屏宽伸缩、尽量占满可用空间，并支持一键清除；同排放抬头、报账人、标签、高级、排序，「全部 / 待补材料」等状态预设单独在第二行；窄屏自动换行和横向滚动；未选择条目时收起不可用的批量按钮。
- 归档批次前展示批次摘要与未齐材料提醒；恢复后直接打开该批次，已归档空状态可一键返回在办；在办无条目且存在已归档批次时，引导前往已归档查看。
- 已归档工作面不再显示条目数量，归档说明改为悬浮提示，并补充界面上的「全选」按钮。
- 使用提示补充批次归档入口：从批次右侧「⋯」菜单进行归档，已归档批次可恢复到在办。
- 打印导出时若当前聚焦某个批次，会自动带入该批次已保存的批次备注，便于生成纸质材料时保留交接信息。

### Fixed

- 设置中打开「使用提示」后关闭，会回到设置，而不再直接回到主界面。

## 2026-08-03

### Added

- 条目卡片的报账人和批次徽章支持左键编辑；批次可新建、切换到其他批次或移出所有批次。
- 发票、付款截图、实物图、查验按钮支持右键打开已有材料，macOS 上 PDF 和图片优先使用系统“预览”。
- 设置新增“扩展”：可修改新建默认抬头，并分别配置付款截图、实物图、查验单和实付金额是否为必需；发票固定必需，默认要求付款截图、查验单和实付金额。
- 批次栏保留“未进批次”文件夹筛选；主界面付款按钮恢复为短标签，启用实物图要求时显示实物快捷按钮并与“详情”同排；详情材料区已上传文件不再叠加背景。

## 2026-07-31

### Fixed

- 发票 PDF 识别进一步兼容字符分散的号码 / 日期、错位的购买方 / 销售方，以及粘连的数量 / 单价 / 金额。

### Added

- 绑定包导入新增确认预览：可查看并修改报账人 / 审核人、给全部条目统一打标签，并选择已有批次、新建批次或不装入批次；确认后才真正导入。

### Changed

- 导出默认名称现在包含本地时间并精确到分钟；设置中的“打开导出目录”会显示目录占用空间，“清理临时文件”移到其旁边，便于集中维护本机文件。
- 导出弹窗现在默认只勾选绑定包，总览 Excel 和附件整理包改为按需选择。
- 设置分区标题改用更适合 Windows 中文界面的字体、字号和字距，不再套用英文式全大写标题样式。

## 2026-07-30

### Fixed

- 打印 Word 现在优先使用条目中已核对的实际物资名称，识别不到发票明细时不再输出“发票物资”；缺失单位默认按“个”输出，验收单单价固定保留两位小数，单价与金额均带“¥”。
- 发票 PDF 明细识别新增按列拆分文本的兜底，并兼容“同品名 + 税率 + 折扣金额 + 折扣税额”顺序的折扣行；现有 80 张发票均能识别品名且明细金额闭合。历史条目缺少已保存明细时，打印导出会从原发票 PDF 补识别，不必删除后重新导入。
- 从源码运行 tidoc 时改为优先使用工作区内的打印组件，不再被数据目录中已安装的旧组件覆盖，避免 Word 格式修改未实际生效。

### Added

- 顶部“识别提醒”视图的批量操作栏新增“重新识别”：勾选条目后可用已有原发票 PDF / XML 重新生成明细并刷新提醒，保留实际物资名称、实付金额和备注。

### Changed

- 设置中的 Bilibili 使用说明入口更新为新版完整操作视频。

## 2026-07-25

### Added

- 绑定包现在显式保存报账人姓名、审核人及每条发票的归属；接收方导入后会复用或创建对应报账人，不再把所有条目统一挂到当前身份。
- 设置新增“绑定包包含备注”和“绑定包包含标签”开关；关闭备注时会同时移除条目备注、附件备注、备注修改历史与汇总备注。
- 设置的关于区新增带图标的 Bilibili 使用说明视频入口。
- 顶部抬头筛选会自动补入数据库中的第三方抬头，并在存在非内置抬头时显示提示点。

### Changed

- 在线查验提示补充说明：查验单 PDF 保存到监测目录后需等待约 1–2 秒，由软件完成识别和归档。
- 打印导出组件改用独立版本；普通核心发布不再同步抬高组件版本，只有 PDF 拼接、付款截图排版、Word 输出、组件依赖或调用协议变化时才提示组件更新。
- 新增第二个报账人或导入后首次达到多个报账人时自动开启代填模式，仍可在设置中手动关闭。
- 设置中的开关状态统一使用“已开启 / 已关闭”文案与对齐样式。
- 设置中原本分别指向同一管理页的“打印导出组件”和“软件更新”合并为一个“软件与组件”入口，同时展示软件版本、组件状态和更新提醒。
- 条目卡片新增所属批次名称；移除与顶部状态筛选及发票 / 实付 / 付款 / 查验快捷按钮重复的“齐备 / 部分材料”徽标。

## 2026-07-24

### Added

- 条目卡片与详情新增「在线查验」：向国家税务总局查验平台预填发票号码、日期和价税合计，验证码由用户在官网填写。查验成功后点击官网「打印」，通过系统打印窗口横向另存为 PDF；保存到下载、桌面或文档目录后自动归入当前条目，无需回到 tidoc 再确认。已有 PDF 仍可手动上传。
- 精简在线查验弹窗：隐藏证书兼容等实现细节，只保留验证码、官网打印和自动归档提示；保存完成后只保留「完成」。
- 兼容 macOS 官网结果页和旧版 PrintArea 的多层 iframe 打印方式；打印任务只在查验窗口中使用横向页面，不影响其他窗口。
- 设置新增「查验单」区块：集中管理额外归档目录，并可选择在成功复制到条目后把外部原 PDF 移到系统废纸篓或回收站。

### Fixed

- 在线查验窗口在初版基础上适当放大但不再默认最大化；预填时通过官网日期控件写入日期、分步触发字段校验并主动收起日历，不再需要用户额外点击空白处确认。
- 查验单改回官网「打印」触发的系统原生横向打印流程，不再使用 WebView2 / WKWebView 的直接 PDF 导出接口；另存为 PDF 后由会话监测自动归入对应条目。
- 在线查验支持选择并记住一个额外的自动归档目录，不再只限于下载、桌面和文档；原生打印的建议文件名改为“查验单-发票号码”，避免连续查验时文件同名。
- 修复 macOS 系统打印未采用网页标题、另存 PDF 仍显示税务平台官网名称的问题；查验窗口现在直接设置原生打印任务名称。
- 查验所需的证书错误放行改为在创建原生窗口前启用，并保持到软件退出，避免 Windows WebView2 初始化较早时遗漏验证码接口的证书处理。
- 自动归档查验单时，只有 PDF 内容或 OCR 读出的发票号与当前条目匹配才会绑定；tidoc 提供的建议文件名只方便保存，不作为归属依据。无法确认归属时保留原文件并提示手动选择。普通启动同时隐藏 macOS 输入法及 pypdf 修复官网打印 PDF 时产生的无害终端警告。
- 修复单独拖入或粘贴已存在的发票 PDF 时只提示失败数量、不显示具体原因的问题；现在失败分组会保留在导入预览中，并提示重复发票号、原销售方和报账人。
- 单条新建与批量、拖拽、粘贴入口统一使用全库重复校验，发票号未识别时以文件内容摘要兜底；导入中途失败会清理已写入的半成品条目和附件，零条目创建时也不再把随附材料误匹配到全库其他条目。
- 修复删除条目后附件目录仍残留、附件复制或替换失败后可能留下半成品文件，以及条目字段初始化失败时事务未回滚的问题。
- 绑定包导入改为整包回滚，重复导入时跳过已有发票；用户确认接收完整性异常的绑定包后，相关条目会实际标记为严重问题。
- 修复“恢复默认数据位置”迁移失败后数据库连接已经关闭、应用无法继续使用的问题。
- 拖拽文件经过条目卡片时不再显示遮挡金额的“拖到这里绑定材料”浮层文字，仍保留卡片描边反馈。
- 付款截图 OCR 自动填写的实付金额会记录来源；删除或改类最后一张付款截图时，未被人工修改的自动值恢复为发票总金额，人工填写的实付保持不变。
- 修复部分 PDF 将字段标签与实际值分段存放时，把“下载次数”等标签误识别为销售方并错配购买方税号的问题。
- 修复商品规格含“合”字（如“25合1”）时被误当成合计行，导致整条商品明细无法识别的问题。
- 付款截图金额识别会优先采用独立金额行和“支付成功 / 交易成功”等上下文，不再把 `ZTPV-25`、`K-5204K` 一类商品型号误识别为付款金额；单张截图金额与发票总额不一致时会弹窗确认，不再直接覆盖实付金额。

## 2026-07-23

### Changed

- 软件更新页会校验打印导出组件的可执行文件与安装完整性；组件缺失或损坏时显示“需要修复”，即使已是最新版本也可主动重新安装。
- 明细识别合计与发票总额不一致改为“识别提醒”，不再标成严重问题或阻断材料齐备；历史条目会自动迁移。
- 设置把付款处理拆成两个独立开关：“付款截图 OCR”关闭后不再识别或套用截图金额，单独拖入截图时直接手动选择条目且不再显示“未识别到付款金额”；“实付默认等于发票金额”控制之后新建或批量导入的条目是自动填写发票金额还是留空待确认。
- 多选条目后可批量修改报账人，并为每条实际变化保留修改记录。

### Fixed

- 修复发布版打印导出组件漏打包 `models`、`builder` 及重依赖，导致拼接 PDF 时提示 `No module named 'tidoc_print.models'` 的问题。
- 发布流程新增打印组件成品自检，Windows / macOS 构建无法完整加载打印模块时不再发布。
- 修复组件文件已删除但软件更新页仍仅凭版本标记显示“已安装”的问题。
- 修复单独拖入或粘贴付款截图时错误沿用上一次批量导入范围，导致明明存在同金额条目却提示“没有金额相同的条目”的问题；Windows 打包版同时为 CSS / JS 资源增加版本标记，避免 WebView2 缓存继续运行旧匹配逻辑。
- 修复 Windows WebView2 把带查询参数的本地入口地址当作文件名，启动时显示“未找到文件”的问题；版本标记只保留在 CSS / JS 资源上。
- 修复 Windows 打包版条目较多时列表高度未正确收缩、无法滚动到底部的问题；材料绑定列表也增加独立滚动区域。
- Windows 付款截图 OCR 会先把手机长截图缩放到系统限制内，再识别完整图和顶部区域，避免超大图片直接识别失败。
- 修复单张发票 PDF 中的 20 位银行账号被误当成第二个发票号码，导致批量导入提示“包含 2 张发票”的问题。

## 2026-07-14

### Changed

- 打印 PDF 默认按条目连续排列发票、付款截图和查验单，旧式按材料类型分别拼接改为可选；条目编号与页码叠加仍默认开启，并可在生成前关闭。
- 自动发现更新弹窗精简重复版本文案，使用指南改为醒目的可折叠完整流程。
- 精简悬浮提示：短标题、销售方、备注和文件名不再重复弹出原文，仅在内容被截断时显示完整文字；状态原因、快捷键、路径和图标按钮说明继续保留。
- “已修改”统一只表示实付金额与发票总金额不一致；其他字段修改继续保留历史记录，不影响原有编辑逻辑。
- 批量拖入付款截图时按 OCR 金额自动绑定到唯一同额条目；识别失败、没有匹配或存在同金额条目时改为逐份手动选择，避免整批误绑到同一条目。
- 详情中删除附件或物品明细后改为原位更新，保留删除提示但不再关闭并重开弹窗。
- 拖拽和粘贴材料、付款截图 OCR、批量扫描与创建、绑定包导入和批量导出新增持续进度提示，后台处理中会显示当前阶段，不再长时间无反馈。

## 2026-07-13

### Fixed

- 修复部分数电发票 PDF 的税额行以 `¥` 结尾时，下一行账号被误识别为价税合计的问题。
- 补充小数点被空格拆散、明细名称跨行以及个人抬头发票的购买方 / 销售方识别兼容。
- 修复商品名在 PDF 首列跨行时只保留第一行的问题，并避免把规格型号续行误拼进商品名；批量导入会跳过包含多张发票的合并 PDF。
- 批量扫描同一发票号的重复 PDF / XML 时只保留一组，重复文件直接提示跳过，不再等到创建条目时失败。

## 2026-07-11

### Changed

- 选中条目的「批次」操作统一管理归属：可装入、补齐、移出，批次内还可原子移动到另一批次；右键单条也可快速处理。
- 标签操作会显示选中条目的已有情况，并新增全库标签重命名、合并和删除入口。
- 顶部关键词搜索支持发票金额和实付金额，并统一搜索、筛选、排序与批量操作的图标和控件层级。

### Fixed

- 修复条目装入批次后只有加入入口、无法从工作界面移出或移动的问题。
- 修复标签只能逐条移除、无法统一修正名称或彻底删除，以及标签筛选在重命名或删除后残留的问题。

## 2026-07-10

### Added

- 软件更新页新增 GitHub 图标与 GitHub Releases 手动下载入口，更新服务不可用时仍可下载安装包。
- 新增可选的启动后自动检查：默认关闭，每 24 小时最多联网一次，只检查版本，不自动下载或安装。
- 自动发现新版本和升级后的首次启动会显示 What’s changed，并在同一弹窗中提供快速开始工作流。
- 设置的高级数据维护可清理拖拽中转文件与旧更新包，同时保留材料、数据库、导出文件和待安装更新包。

### Changed

- 软件更新页改为紧凑的版本台账，突出当前版本、最新版本、包体积、版本变化和下一步动作，移除“当前平台”和更新清单地址等低价值信息。
- Windows 发布包改为按用户安装器，并将 PyInstaller 核心构建改为 onedir 后再压缩安装，降低日常启动时的一次性解包开销。
- GitHub Actions 自动从 CHANGELOG 最新一节生成客户端更新说明与 GitHub Release notes，再构建安装包、更新清单并发布到 COS / GitHub。

## 2026-07-09

### Added

- 支持拖拽 / 粘贴混合材料：发票进入导入预览，能按发票号唯一匹配的查验单会在创建条目后自动绑定，未匹配材料再手动选择条目。
- 付款截图添加时会用系统本地 OCR 尝试识别实付金额；单张可直接填入，多张会提示确认合计，已有手动实付金额时不覆盖。
- 批量创建条目接口返回创建结果明细，便于前端把后续材料绑定到新条目。

### Changed

- 设置里的「收款信息」收进「数据位置」区块，和本机数据、导出目录、迁移维护放在一起。
- 付款截图拼接 PDF 的叠加编号改为与发票 / 查验单一致的 `No.` / `Page` 风格。
- README 和设计文档补充当前材料绑定、付款截图 OCR 与轻量系统 OCR 的真实边界。

### Fixed

- macOS 上 Python 证书链无法验证更新源时，更新下载会改用系统信任链重试，避免已安装系统根证书但应用内 OpenSSL 仍报错。

## 2026-07-08

### Added

- 新增「报账批次」：运营组把一次要交的材料自由圈选、命名、留存为一个集合，可跨报账人、跨抬头、只取某人部分条目；主界面以文件夹式批次条显示条数与缺件数。
- 批次内每条可写「批次级催办备注」（如「张三缺查验单」），与条目自身的记账备注分离。
- 批次上下文条显示每报账人小计；导出 / 打印统一从选中条目的操作条进入。
- 条目支持打多个标签，并可批量打标 / 去标；高级筛选新增按条目标签、按有 / 无条目备注、按批次筛选。
- 主界面支持多栏条目列表、Shift 范围选择，并扩大复选框 / 抬头色条的选择热区，减少误打开详情。
- 报账人支持编辑姓名和审核人；设置新增「收款信息」用于打印 Word 的姓名、学号、电话、开户行、卡号。
- 设置里可把数据整体迁移到自定义位置，或恢复到系统默认位置。
- 设置新增启动默认抬头、默认列表密度偏好。

### Changed

- 设置页重构为配置中心（报账人、收款信息、偏好、数据位置、组件与更新、关于），移除与主界面重复的入口（批量导入 / 导入绑定包）和开发者视角的内部路径。
- 顶栏动作按主次重排：导入 / 设置归为次级，批量导入 / 新建为主操作；抬头、报账人、批次、状态、排序统一收到筛选工具条。
- 打印导出的发票 / 查验单拼接 PDF 改为右下角份数页码；付款截图改为横版 A4 每页两张，输出完成后可直接打开所在文件夹。
- 设置中的默认抬头和默认列表密度调整后会立即同步到主界面。
- 优化主界面字体、金额数字、条目选中态和首次使用提示的视觉层级。
- 数据库 schema 升级到 v2，历史库首次打开时自动补齐批次相关表。

## 2026-07-07

### Added

- 新增 MIT 开源许可证、贡献指南与 GitHub Issue / PR 模板。
- 新增 CI 工作流，在 macOS / Windows 上运行 pytest。

### Changed

- 重写 README.md，按当前代码实际情况整理项目介绍、使用要点与状态说明。
- 在设计文档中注明 OCR 识别组件暂缓、核心流程已实现。

## 2026-07-06

### Added

- 支持从文件夹或多选文件批量导入发票 PDF，并自动匹配同目录或子目录中的发票 XML。
- 支持将发票 PDF / XML 拖到主界面进入批量导入预览。
- 支持将付款截图、查验单拖到条目卡片或条目详情材料区绑定到指定条目。
- 新增首次使用提示，并在设置中提供“使用提示”入口。
- 新增拖拽中转区 `dropped/`，用于 WebView 拿不到本机路径时保存临时文件。

### Changed

- 批量导入只用发票 PDF 创建条目，XML 只作为辅助识别；付款截图和查验单不再按文件名自动绑定。
- 优化拖拽视觉反馈：空白列表区用于批量导入，条目卡片用于绑定材料，减少误拖。
- 设置页重组为常用操作、功能状态、本机数据，减少重复入口。
- 精简列表模式会收起次要信息和快捷按钮，而不只是压缩间距。
- 教育基金会条目只保留颜色和色条区分，不再额外强化显示抬头标签。

### Fixed

- 修复部分数电发票 PDF 文本流错位导致的购买方、销售方、明细和金额识别失败。
- 修复拖拽提示在 WebView 中可能不消失的问题。
- 修复 pywebview 文件对话框弃用警告，改用新版 `FileDialog` API。
- 过滤 macOS 上已知无害的 `NSSoftLinking` 输入法框架警告。
- 防止同一条目重复上传同一份附件。
- 防止将不同发票号的 PDF / XML 拖入已有条目造成误绑定。
