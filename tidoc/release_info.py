"""由 scripts/set_version.py 生成的当前核心版本发布说明。"""

RELEASE_VERSION = '0.1.37'
RELEASE_NOTES = [
  "绑定包导入预览的条目列表改为固定共用列宽，金额、归属和处理状态不再随文案长度错位；每条同时显示「新增 / 补充 / 已有」、可补充内容与本机已有批次。默认仍沿用统一导入设置；仅在需要时可逐条取消勾选，或将个别条目单独放入已有批次、新批次或「未进批次」。",
  "大型绑定包的完整性检查改为流式校验附件、复用已解压的结构数据，并批量读取本机重复条目的对比信息，减少重复读取、数据库查询与内存占用。用户确认导入时会复用未变更文件的已验证结果，不再把整包重复校验一遍。"
]
